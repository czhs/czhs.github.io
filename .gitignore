_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
assets/libs/
node_modules/
vendor
.idea
.venv

# local jekyll-serve overlay (excludes large untracked robotics_media/)
_config_local.yml

# Local-only tooling, never published (e.g. update_mln_from_luma.sh).
/local/

# Portfolio micro-sites (robotics + ml) publish as under-construction shells
# (pages/layouts/data are tracked). Their raw + processed MEDIA stays local-only
# and uncommitted until each project is ready to go public.
/robotics_media/
/assets/img/robotics/
/assets/video/robotics/
/assets/img/ml/
/assets/video/ml/
