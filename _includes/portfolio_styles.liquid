<style>
/* ── Robotics portfolio — standalone micro-site ───────────────── */
body.rbx-site { padding: 0; }

/* two-column shell: fixed-ish sidebar + project grid */
.rbx-wrap {
  display: grid;
  grid-template-columns: 320px minmax(0, 1fr);
  gap: 3rem;
  max-width: 1500px;
  margin: 0 auto;
  padding: 2.75rem 2.75rem 4.5rem;
}

/* ── sidebar ──────────────────────────────────────────────────── */
.rbx-side {
  position: sticky;
  top: 2.75rem;
  align-self: start;
  display: flex;
  flex-direction: column;
  min-height: calc(100vh - 5.5rem);
}
.rbx-mark-link {
  display: inline-flex;
  color: var(--global-theme-color);
  margin-bottom: 2rem;
}
.rbx-mark-link:hover { opacity: 0.8; }
.rbx-mark { display: block; }
.rbx-name {
  font-family: var(--global-code-font-family, monospace);
  font-weight: 800;
  font-size: 2.6rem;
  line-height: 1.05;
  letter-spacing: -0.01em;
  margin: 0 0 1rem;
  color: var(--global-text-color);
}
.rbx-sub {
  font-size: 0.98rem;
  line-height: 1.45;
  color: var(--global-text-color);
  opacity: 0.6;
  margin: 0 0 1.1rem;
  max-width: 22rem;
}
.rbx-uc {
  align-self: flex-start;
  font-family: var(--global-code-font-family, monospace);
  font-size: 0.72rem;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  padding: 0.3rem 0.65rem;
  border: 1px solid var(--global-divider-color);
  border-radius: 999px;
  opacity: 0.85;
}

/* link list, pinned to the bottom of the sidebar */
.rbx-links {
  display: flex;
  flex-direction: column;
  margin-top: auto;
  padding-top: 2.5rem;
}
.rbx-link {
  padding: 0.85rem 0;
  border-top: 1px solid var(--global-divider-color);
  color: var(--global-text-color);
  text-decoration: none;
  font-size: 0.95rem;
  opacity: 0.85;
  transition: opacity 0.15s ease, color 0.15s ease;
}
.rbx-link:hover { opacity: 1; color: var(--global-theme-color); text-decoration: underline; }

/* ── project grid ─────────────────────────────────────────────── */
.rbx-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 3rem 2.25rem;
  align-content: start;
}
.rbx-card {
  display: block;
  color: var(--global-text-color);
  text-decoration: none;
}
.rbx-card:hover { text-decoration: none; }
.rbx-cover {
  display: block;
  width: 100%;
  aspect-ratio: 4 / 3;
  object-fit: cover;
  border-radius: 8px;
  background: var(--global-card-bg-color);
}
.rbx-cover-empty {
  border: 1px dashed var(--global-divider-color);
  display: flex;
}
.rbx-cover-empty::after {
  content: "under construction";
  margin: auto;
  font-family: var(--global-code-font-family, monospace);
  font-size: 0.78rem;
  letter-spacing: 0.04em;
  opacity: 0.4;
}
.rbx-cap {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 1rem;
  margin-top: 0.85rem;
}
.rbx-card-title {
  font-weight: 600;
  font-size: 1.05rem;
  border-bottom: 1px solid transparent;
}
.rbx-card:hover .rbx-card-title {
  border-bottom-color: currentColor;
}
.rbx-card-date {
  flex-shrink: 0;
  font-size: 0.9rem;
  opacity: 0.55;
}

/* ── detail page ──────────────────────────────────────────────── */
.rbx-postwrap {
  max-width: 720px;
  margin: 0 auto;
  padding: 2.75rem 1.5rem 4.5rem;
}
.rbx-back {
  display: inline-block;
  margin-bottom: 2.5rem;
  font-family: var(--global-code-font-family, monospace);
  font-size: 0.85rem;
  letter-spacing: 0.03em;
  color: var(--global-text-color);
  text-decoration: none;
  opacity: 0.7;
}
.rbx-back:hover { opacity: 1; color: var(--global-theme-color); }
.rbx-post-title {
  font-family: var(--global-code-font-family, monospace);
  font-weight: 800;
  font-size: 2.2rem;
  line-height: 1.1;
  letter-spacing: -0.01em;
  margin: 0 0 0.4rem;
}
.rbx-post-meta {
  font-size: 0.95rem;
  opacity: 0.55;
  margin: 0 0 2rem;
}
.rbx-post-video {
  display: block;
  max-width: 100%;
  max-height: 78vh;
  margin: 0 auto 2rem;
  border-radius: 10px;
  background: #000;
}
.rbx-post-hero {
  display: block;
  width: 100%;
  margin: 0 0 2rem;
  border-radius: 10px;
}
/* Paper · Code · Demo link row */
.rbx-post-links {
  display: flex;
  flex-wrap: wrap;
  gap: 0.6rem;
  margin: 0 0 2.2rem;
}
.rbx-post-link {
  font-family: var(--global-code-font-family, monospace);
  font-size: 0.82rem;
  letter-spacing: 0.02em;
  padding: 0.45rem 0.9rem;
  border: 1px solid var(--global-divider-color);
  border-radius: 999px;
  color: var(--global-text-color);
  text-decoration: none;
  opacity: 0.9;
  transition: opacity 0.15s ease, color 0.15s ease, border-color 0.15s ease;
}
.rbx-post-link:hover {
  opacity: 1;
  color: var(--global-theme-color);
  border-color: var(--global-theme-color);
  text-decoration: none;
}
.rbx-post { font-size: 1.02rem; line-height: 1.7; }
.rbx-post h2 {
  font-size: 1.25rem;
  font-weight: 700;
  margin: 2.2rem 0 0.6rem;
}
.rbx-post p { margin: 0 0 1.1rem; }
.rbx-post img {
  display: block;
  width: 100%;
  border-radius: 8px;
  margin: 1.5rem 0;
}
.rbx-post-foot {
  margin-top: 3.5rem;
  padding-top: 1.25rem;
  border-top: 1px solid var(--global-divider-color);
  font-size: 0.82rem;
  opacity: 0.5;
}

/* ── responsive: stack sidebar above the grid ─────────────────── */
@media (max-width: 820px) {
  .rbx-wrap {
    grid-template-columns: 1fr;
    gap: 2.5rem;
    padding: 2rem 1.5rem 3.5rem;
  }
  .rbx-side {
    position: static;
    min-height: 0;
  }
  .rbx-links { padding-top: 1.75rem; }
  .rbx-grid { grid-template-columns: 1fr; gap: 2.25rem; }
  .rbx-name { font-size: 2.2rem; }
}
</style>
